services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: metalsense-backend

    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env

    restart: unless-stopped
